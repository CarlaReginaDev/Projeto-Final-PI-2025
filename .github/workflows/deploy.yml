name: Projeto Final PI 2025

on:
  release:
    types: [published]    
  push:
    branches:
      - tests
jobs:
  deploy:
    runs-on: ubuntu-latest
    needs: tests
    permissions:
      id-token: write
steps:
  - name: Faz Checkout do Código
    uses: actions/checkout@v5
  - name: Configura Python
    uses: actions/setup-python@v5
    with:
      python-version: 3.14